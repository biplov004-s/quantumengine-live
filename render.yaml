services:
  - type: web
    name: quantumengine-api
    runtime: python
    buildCommand: pip install -r backend_requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: REFRESH_TOKEN
        generateValue: true
